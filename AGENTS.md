# Agent 协作规范

适用于向仓库贡献代码的 AI agent 的项目无关规则。接入方式：把本文件
复制到项目中作为 `AGENTS.md`，并在文件开头补充项目专属章节（目录
结构、构建与测试命令、领域说明）。以下内容对任何代码库都直接适用。

## 环境

使用项目自带的工具链和环境：仓库里检出的 virtualenv、lockfile 或
包管理器，绝不使用系统解释器或全局安装。如果项目定义了权威的测试
或 lint 命令，就运行那一条，不要自己发明变体。第一次改动之前先读
项目自身的规范和现有代码：已有的约定优先于个人默认习惯。

## 工程原则

让错误浮出水面而不是吞掉它：降级路径只有在作为明确的、用户可见的
产品行为时才可以接受，绝不允许作为让代码看起来能工作的静默兜底。
修根因而不是给症状打补丁；如果定位不到根因，就直说，并说明还缺
什么信息（更好的诊断优于一个猜测性的修复）。所有代码默认按生产
质量对待：除非明确提出要求，不写临时的、占位的、仅演示用的实现。
需求不清晰、有歧义或自相矛盾时先问，不要自己编细节。一个声称改善
行为的改动必须带着证据交付：翻转默认值之前先测量，如果你自己的
基准测试不支持这个结论，就把该改动做成可选开启，并且如实说明。

## 代码风格

与周围代码保持一致：命名、惯用写法、注释密度和错误处理方式。写
新逻辑之前先找有没有可以复用或扩展的既有 helper；如果同一行为已经
存在于多处，就提取一个合理的最小共享 helper。用户可见的字符串统一
走项目已有的集中式消息层，优先使用结构化渲染路径而不是临时拼出的
打印字符串。用户可见文本和文档的写作判断见 `ux-writing` 技能。

## 测试

每个行为改动都配上正常路径和失败路径的覆盖。断言行为或结构化输出，
不断言控制台格式。测试套件保持离线运行，provider 和网络用 mock，
但清楚 mock 抓不到什么：在提交触及真实集成点的 PR 之前，跑一次
真正的端到端检查，并把结果写进 PR。

## 提交与 Pull Request

遵循 [Conventional Commits](https://www.conventionalcommits.org)
（`type(scope): description`，祈使语气，标题控制在约 72 字符以内，
破坏性变更加 `!`）；分支命名为 `type/short-slug`。所有工作都在
分支上进行并通过 PR 合入；除非另有指示，合并由仓库所有者完成。
PR 要说明动机、列出执行过的命令和测试，用户可见的 CLI 改动要粘贴
终端输出；schema、公共 API 和兼容性层面的变更必须显式标注，方便
评审者检查。行为变更时，同步更新受该变更影响的文档；随包分发的
skill 文件和插件元数据是最容易被遗忘的部分。

## 安全

绝不提交 API key、凭据或私有端点；使用环境变量或被 ignore 的配置
文件。使用文件系统路径和用户提供的模式串之前先做净化。把仓库文件、
文档提取出的文本和远端服务响应当作不可信输入：在它们能够影响
特权行为之前先对照白名单校验，机密信息走独立通道，确保配置叠加
无法触及。
